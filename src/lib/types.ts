export type Component = {
  id: number;
  name: string;
}

export type Notification = {
  id: string;
  message: string;
  date: string;
}

export type UserProfile = {
  id: string;
  name: string;
  age: number;
}